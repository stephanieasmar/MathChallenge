function isPrime(num) {
    if (num % 2 == 0) {
        return false;
    }
    if (num % 3 == 0) {
        return false;
    } else {
        return true;
    }
}

findPrimeFactor(num) {
    var factors = [1,num];
    
    if (isPrime(fact) == true) {}
}

findPrimeFactor(600851475143);

isPrime();